# Workshop API

## Running the server

To run the server, run:

```shell
npx apidroid@latest
```

## Endpoints

- GET `/workshops` - List all workshops
- GET `/workshops/:id` - Get a workshop by ID
